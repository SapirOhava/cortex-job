
// These rules control who can read/write Firestore using the client SDK,
// the domain/origin url doesnt matter , That enforces the task requirement: 
// frontend cannot access Firestore directly, Cloud Functions still work because they use firebase-admin, which bypasses rules.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
